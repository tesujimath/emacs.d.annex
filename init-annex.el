;;; init-annex.el --- Load the annex to the configuration
;;; Commentary:

;; This file loads the extras, which are kept separately.

;;; Code:

(require 'init-tesujimath)

(provide 'init-annex)

;;; init-annex.el ends here
